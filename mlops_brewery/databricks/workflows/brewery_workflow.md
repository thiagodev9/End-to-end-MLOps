O Workflow conecta as tasks do Job, garantindo que:

A ingestão rode primeiro

O preprocessamento só execute após ingestão

O treinamento só execute após os dados estarem prontos

Esse padrão é comum em Databricks Workflows.


The Workflow connects the tasks and makes sure that:

Ingestion runs first

Preprocessing runs after ingestion

Training runs after data is ready